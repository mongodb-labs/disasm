## Interactive Disassembler GUI

Before running, replace the upload path and source code directory path in `config.py` with the appropriate relative paths on your machine.

To run
```python
python app.py
```

### Known Bugs

